pipeline {
    agent any

    tools {
        nodejs 'Nodejs' 
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run Frontend Tests') {
            steps {
                dir('client') {
                    sh 'yarn install'
                    sh 'npm test'
                }
            }
        }
    }
}
